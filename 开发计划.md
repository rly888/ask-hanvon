# 问小汉 · 智能阅读与图书推荐系统 — 开发计划

> 依据《问小汉 - 智能阅读与图书推荐系统（生产级架构图）》分析整理。
> 文档性质：开发路线与任务拆解；架构细节以架构图为准，选型建议为参考，需按 Phase 0 POC 结果拍板。
> 本文档已收录架构评审结论：原架构图只覆盖在线服务路径，缺失能力（离线数据链路/模型治理/评估门禁/安全专项/服务拓扑）的增补设计见 §3。

---

## 0. 一句话定位

这不是「接一个大模型 API」的应用，而是 **「Agent 编排 + RAG + 推荐排序」三引擎协同的图书垂直领域智能系统**。
差异化与工作量集中在业务应用层；基础设施层（k8s/Nacos/SkyWalking/ELK 等）是标准 Java 微服务全家桶，照抄最佳实践，不做自研。

**核心开发策略：先打通一条主链路 → 横向扩展 → 生产化。不要一开始就拆 20 个微服务。**

---

## 1. 架构总览（分层解读）

```
用户 → 5 端(电纸书/手机/小程序/Web/音箱) → 接入层(CDN/DNS/WAF/SLB)
  → 网关(Spring Cloud Gateway / Kong: 路由·鉴权·限流·熔断·黑白名单·审计日志)
  → 业务应用层(问小汉核心能力):
      ├─ 对话与交互(多轮对话管理/会话记忆/用户画像感知/意图识别/Query理解与改写)
      │     └─→ 问小汉 Agent 大脑(编排层): Planner → Tool Selector → Executor → Synthesizer
      │           ├─ 短期记忆(Redis)         ├─ 长期记忆(向量/图存储)
      │           └─ 工具调用 → 工具中心(Tool Center) ⇄ MCP Server
      ├─ 检索增强(RAG)引擎: 混合检索(BM25@ES + 向量@Milvus) → 召回融合 → Rerank → TopK → 上下文构建
      ├─ 推荐排序引擎: 特征提取 → 粗排召回 → 精排(LTR) → 业务规则重排 → 最终结果
      ├─ 生成与回答引擎: LLM(DeepSeek) + 生成增强 → 结果生成 → 引用/溯源(Citation)
      └─ 运营与策略中心: 推荐策略配置 / Campaign 管理 / 优先图书配置 / 权重策略配置 /
                         A-B 测试 / 长尾人群配置 / 策略效果分析
  → 数据层:
      ├─ 结构化: 图书库(MySQL/PG) · 用户会员库 · 订单/交易库 · 运营配置库
      ├─ 非结构化: 图书内容库(TXT/PDF/EPUB 等) · 封面/图片/多媒体 · 评论/书评/问答
      ├─ 向量/索引: 向量索引库(Milvus) · 全文检索(Elasticsearch) · 图数据库(Neo4j)
      └─ 缓存/消息: Redis(缓存/会话/记忆) · 消息队列(Kafka/RabbitMQ)
  → 基础设施层: 云平台/私有化机房 · Docker/k8s · Nacos · Apollo · SkyWalking ·
                Prometheus+Grafana · ELK · 备份容灾
侧柱: 运维与治理(监控大盘/告警/链路追踪/日志/容量/灰度/故障自愈/安全审计)
      数据治理(数据质量/数据安全/隐私保护/合规管理)
```

### 1.1 关键判断

| # | 判断 | 影响 |
|---|------|------|
| 1 | **两条主链路形态完全不同** | 问答链路（RAG）在线检索增强，对引用准确性敏感；推荐链路离线特征 + 在线打分，延迟敏感（<100ms）。不能混在一个引擎里 |
| 2 | **Agent 是编排中枢而非核心能力** | 搜索/阅读/推荐/订单都是工具；工具化程度决定系统上限，工具中心 schema 最先设计 |
| 3 | **MCP Server 双刃剑** | 既可将工具中心开放给外部 Agent 复用，也可能引入第三方工具。建议工具层与 Agent 层解耦，按 MCP 规范定义、独立测试 |
| 4 | **引用/溯源是生命线** | 答案必须能定位到书内章节/页码，否则幻觉不可控，版权合规也过不去 |
| 5 | **内容解析是最大脏活** | 图书数据的解析/chunk/embedding pipeline 是 RAG 质量上限，也是最长关键路径，必须最早立项 |
| 6 | **原图只有在线服务路径** | 精排模型、向量索引、用户特征、效果回流都是离线计算，原图完全缺失；增补设计见 §3 |
| 7 | **分层图 ≠ 服务拓扑图** | 原图看不出服务边界、调用关系与数据所有权；目标服务拓扑见 §3.5，作为 Phase 3 拆分依据 |

### 1.2 端到端时序（两条主链路）

**问答链路（RAG 主链路）**

```
用户提问 → 接入/网关 → 对话与交互(意图识别: 判定为内容问答)
  → Agent 大脑(Planner 规划 → Tool Selector 选择「图书阅读问答」工具)
  → 工具中心 → RAG 引擎:
      Query 理解/改写 → 混合检索(BM25@ES + 向量@Milvus) → 召回融合 → Rerank
      → TopK → 上下文构建(带书内 locator 元数据)
  → 生成引擎(LLM + 引用格式化) → 流式返回 → Synthesizer 结果整合
  → 会话记忆写入(Redis 同步 + 长期记忆异步) → 用户
```

**推荐链路（在线打分 + 离线闭环）**

```
离线侧: 行为埋点 → Kafka → 特征计算 → 召回候选集预计算 / 精排模型训练 → 在线模型库
在线侧: 用户请求 → 意图识别(推荐) → Agent → 推荐工具 → 推荐排序引擎:
          实时特征拼接 → 多路召回 → 精排打分 → 业务规则重排 → 结果(带解释理由)
用户反馈(点击/收藏/购买) → Kafka → 效果回流 → A/B 分析 → 策略中心闭环
```

---

## 2. 逐层开发要点（按 ROI 排序）

> 下表覆盖原架构图已有层次；原图缺失的离线链路、模型治理、评估、安全见 §3。

| 层 | 关键组件 | 开发要点 |
|---|---|---|
| 终端层 | 电纸书/手机/小程序/Web/音箱 | 统一 API 契约（BFF + OpenAPI）；音箱走语音流式；小程序受审核合规约束；电纸书考虑离线/低功耗与端云协同（离线缓存问答） |
| 接入层 | CDN/DNS/WAF/SLB | 静态资源与 API 分离；**LLM 流式（SSE）需单独调 LB/网关超时与缓冲**；WAF 对流式响应的影响需实测 |
| 网关层 | Spring Cloud Gateway / Kong | 鉴权统一 JWT；限流按用户/接口多维度；熔断给降级话术（"我暂时查不到"）而非报错；审计日志记录工具调用 |
| 对话与交互 | 意图路由 | 问「书的内容」→ RAG；问「读什么」→ 推荐；闲聊/笔记/买书 → 各自分流。**这是最重要的一次分流设计** |
| Agent 大脑 | Planner/Tool Selector/Executor/Synthesizer | 起步用轻量自研编排（工具调用循环 + prompt 分步），勿先上重框架；复杂 DAG 再考虑 LangGraph。短期记忆 Redis 带 TTL；长期记忆按用户沉淀（偏好/阅读历史） |
| 工具中心 | 书籍搜索/藏书库/推荐/阅读/用户画像/图书比较/订单购买 | 首批 3 个工具：**书籍搜索、图书阅读问答（RAG 封装）、推荐（规则版）**。订单/购买类工具必须绑定用户身份 + 风控 + 二次确认 |
| RAG 引擎 | 混合检索 + Rerank + 上下文构建 | **chunking 策略**（按卷/章/节/页，带 locator 元数据）；混合检索权重可配置；Rerank 用本地小模型（bge-reranker 系）；上下文控制 token 预算、去重 |
| 推荐引擎 | 召回 → 精排 → 规则重排 | 冷启动靠内容 + 图谱 + 规则（图书无行为数据是天然难点）；精排需离线训练 → 在线打分全链路（见 §3.1），先规则版占位 |
| 生成与回答 | LLM + 引用溯源 | 强制引用格式（书内位置）；低成本 LLM（DeepSeek）+ 结果缓存；**不要将整本正文喂给 LLM 生成镜像内容（版权红线）**；输出过审核通道（见 §3.4） |
| 数据层 | 结构化/非结构化/向量索引/缓存消息 | 内容解析 pipeline 是最大工程量：EPUB/PDF/TXT → 去重 → 章节化 → chunk → embedding → 同步 ES/Milvus；Neo4j 服务「图书-作者-标签-用户」关系推导与推荐解释 |
| 基础设施 | k8s/Nacos/Apollo/SkyWalking/监控/日志/容灾 | 无差异化，照抄最佳实践，不做自研 |

---

## 3. 关键增补能力设计（原架构图缺口）

> 架构评审结论：原图只画了在线服务路径，以下能力在生产级系统里必不可少，按优先级排序。

### 3.1 离线数据链路（最大缺口，优先级最高）

原图中「精排模型（学习排序）」没有来源——模型、特征、索引、效果回流全部是离线计算，需要一条完整的离线层：

- **特征平台**：用户行为 → 特征（特征注册、血缘管理、离线/在线一致性校验）
- **内容处理 pipeline**：EPUB/PDF/TXT → 解析 → 去重 → 章节化 → chunk → embedding → 索引构建（ES/Milvus 增量同步、版本管理）
- **模型训练与 serving**：精排（LTR/CTR）离线训练 → 模型管理 → 在线打分服务；embedding/rerank 模型 GPU 推理服务（vLLM/Triton）
- **批流处理（Flink/Spark）**：行为聚合、召回候选集预计算、效果统计
- **实验效果回流**：A/B 结果 → 策略中心，形成运营闭环

> Phase 安排：最小版 Phase 2 起步（特征 + 离线训练），Phase 3 完善平台化。

### 3.2 模型网关与模型治理

原图 LLM 直连生成引擎，生产级需要一层模型网关：

- **统一接入**：LLM/embedding/rerank 统一 API，屏蔽供应商差异
- **多模型路由与分级**：强模型（复杂推理）/ 弱模型（简单任务兜底）；故障自动降级链路
- **配额与成本计量**：按用户/场景/日配额；成本看板与预算告警
- **审计**：所有模型调用与工具调用日志可追溯

### 3.3 评估与上线门禁

三个引擎各需一套评测，作为**上线门禁**（没有门禁，Phase 验收就会拍脑袋）：

| 引擎 | 评测集 | 指标 |
|---|---|---|
| RAG | 图书问答黄金标注集（问题-答案-引用位置） | 引用准确率、召回率、答案质量（LLM-as-judge + 人工抽检） |
| 推荐 | 离线样本集 + 在线实验 | 离线 AUC/NDCG/覆盖率/多样性；在线 A/B CTR/转化 |
| Agent | 任务用例集 | 工具调用成功率、任务完成率、幻觉率 |

- 门禁规则：**每次模型/索引/策略变更跑回归评测，不达标不上线**
- 评测集建设必须 Phase 0 启动，滞后代价极大（见 §7 风险）

### 3.4 安全专项

原图只有接入层 WAF/黑白名单，Agent 应用有自己的安全面：

- **Prompt injection 防护**：书内容/用户输入注入检测；工具调用参数校验与白名单
- **工具级权限 RBAC**：订单/购买类工具绑定用户身份 + 风控 + 二次确认（防止 Agent 幻觉下单）
- **输出内容审核**：生成结果过审核通道（合规/青少年向/版权红线）
- **防刷与风控**：用户/IP/接口多维度限流与风控规则

### 3.5 目标服务拓扑（Phase 3 拆分依据）

```
接入/网关(SCG/Kong)
├─ 对话交互服务(会话/意图/画像感知)   —— 拥有: 会话数据
├─ Agent 编排服务(Planner/Executor)   —— 无状态
├─ 工具中心(搜索/阅读/推荐/订单工具)  —— 调用下游, 不持有业务数据
├─ RAG 检索服务(混合检索/Rerank)      —— 拥有: 索引与检索逻辑
├─ 推荐服务(在线排序)                 —— 拥有: 特征拼接与打分
├─ 生成服务(LLM 编排 + 引用)          —— 无状态
├─ 离线计算(特征/训练/索引构建)       —— 拥有: 特征与模型
└─ 运营策略中心                       —— 拥有: 策略配置与实验
数据所有权: 图书/用户/订单域各自持库; 其他服务一律通过 API/MQ 访问, 禁止跨库直连
```

---

## 4. 技术选型建议（Phase 0 需 POC 拍板）

| 决策项 | 推荐 | 备选 / 说明 |
|---|---|---|
| LLM | DeepSeek（架构图已定） | 注意 function calling 与 JSON 输出稳定性；流式与工具调用组合需实测 |
| 模型网关 | LiteLLM / One-API，或自研轻量层 | 统一 LLM/embedding/rerank 接入、路由、配额、降级（§3.2） |
| Agent 编排 | 轻量自研（起步） | LangGraph（任务复杂后引入）；统一走 MCP 工具规范 |
| 向量库 | 直接 Milvus 或 ES kNN | 数据量 < 千万级可用 ES kNN 少一套组件；有运维能力则一步到位 Milvus |
| Rerank | 本地小模型（bge-reranker 系） | 通用 LLM rerank 成本高，仅 POC 对比 |
| 模型 serving | vLLM / Triton | embedding、rerank、精排模型的 GPU 推理服务（§3.1） |
| 全文检索 | Elasticsearch（BM25） | 与向量检索共用 ES 可简化部署（kNN + BM25 混合） |
| 离线计算 | Spark/Flink + 特征平台 | 特征计算、模型训练、索引构建、效果统计（§3.1） |
| 图数据库 | Neo4j | 用户-图书-作者-标签关系，推荐解释 |
| 缓存/记忆 | Redis | 短期记忆 TTL；会话级缓存；结果缓存 |
| 消息队列 | Kafka / RabbitMQ | 行为埋点异步链路、内容解析任务队列 |
| 服务框架 | Spring Cloud 全家桶（既有生态） | 网关用 SCG 或 Kong；Nacos 注册配置（或 Apollo） |
| 评测 | 自研评测集 + LLM-as-judge | RAG/Agent/推荐三套评测 pipeline（§3.3） |
| 前端 | 端上按现有技术栈 | H5/小程序/App 复用；统一 OpenAPI 契约 |

---

## 5. 开发路线图

### Phase 0 · 打地基（1–2 周）

**目标：数据与选型先行，避免返工。**

- [ ] 图书元数据 + 内容解析 pipeline 立项（EPUB/PDF/TXT → 去重 → 章节化 → chunk → embedding → 同步 ES/Milvus）—— **最重关键路径，必须最早立项**
- [ ] 用户行为埋点与事件模型设计（浏览/搜索/阅读时长/收藏/购买 → 特征 → 推荐）
- [ ] **RAG 评测集建设启动**：图书问答黄金标注集（首批 100 题：问题-答案-引用位置）
- [ ] 选型 POC：LLM function calling 稳定性、向量库（Milvus vs ES kNN）、Rerank 模型对比
- [ ] 工具中心 schema 先行：列出所有 MCP 工具的输入/输出契约、权限边界（文档先行）
- [ ] 仓库与工程脚手架：单体多模块（为后续按域拆分留边界）

**验收指标**：
- 内容解析覆盖率 ≥ 95%（100 本真实样书抽检）
- 埋点事件模型定稿评审通过；选型 POC 报告归档
- 评测集 100 题定稿并入库

### Phase 1 · MVP 端到端跑通（4–6 周）

**目标：用户问「《三体》讲了什么」→ 拿到带书内引用的回答；首页「猜你喜欢」→ 规则版推荐。**

- [ ] 意图识别（简化版）：问答 / 推荐 / 闲聊 分流
- [ ] Agent 最小循环：Planner（单工具场景直接路由）→ Executor → Synthesizer，轻量自研
- [ ] 工具中心首批 3 工具：书籍搜索、图书阅读问答（RAG 封装）、推荐（规则版）
- [ ] RAG 最小链路：ES BM25 检索（可先不做向量）→ TopK → 上下文构建 → 生成带引用回答
- [ ] 生成引擎：DeepSeek 接入 + 引用格式化（书内位置标注）
- [ ] 最小评测 pipeline：RAG 100 题回归（后续每次变更必跑）
- [ ] 单体部署到测试环境；前端先做 Web/H5 最小页面
- [ ] 推荐规则版：分类/编辑推荐 + 简单协同（或先人工配置位）

**验收指标（上线门禁建议）**：
- 引用位置准确率 ≥ 80%（评测集回归）
- 回答 P95 延迟 ≤ 8s；流式首 token ≤ 2s
- 无法回答时拒答率 ≥ 90%（宁拒答不编造）
- 推荐位结果可解释，规则命中 100% 可追踪

> 备注：搜索工具 + 问答 + 引用格式化本身一天可出 demo，Phase 1 的时间主要在内容库与体验打磨。

### Phase 2 · 体验与效果提升（4–8 周）

**目标：从「能用」到「好用」。**

- [ ] 多轮对话管理 + 会话记忆（Redis 短期 TTL + 用户长期向量/图记忆）
- [ ] 混合检索（BM25 + 向量）+ 召回融合 + Rerank + 上下文构建优化（token 预算/去重）
- [ ] 用户画像感知：阅读偏好、历史行为注入对话与推荐
- [ ] **离线链路起步**：特征计算 → 多路召回 → 精排（LTR/CTR）训练与在线打分 → 业务规则重排
- [ ] **模型网关接入**：多模型路由、分级兜底、配额与成本计量
- [ ] 工具中心补齐：藏书库、图书比较、订单/购买（含身份绑定 + 风控 + 二次确认）
- [ ] 流式输出体验；引用溯源严格化（多片段交叉验证）
- [ ] 运营与策略中心雏形：策略配置化、A/B 实验、效果回流

**验收指标**：
- RAG 引用准确率 ≥ 90%；多轮上下文连贯率抽检 ≥ 85%
- 推荐离线 AUC ≥ 0.70（或 NDCG 提升 ≥ 5%）；在线 A/B CTR 提升 ≥ 3%
- Agent 工具调用成功率 ≥ 95%

### Phase 3 · 生产级（持续）

**目标：可运维、可治理、可扩展。**

- [ ] 按域拆分微服务（按 §3.5 目标拓扑），MQ 解耦，数据所有权落地
- [ ] 网关统一：SCG/Kong + JWT 鉴权 + 限流熔断 + 黑白名单 + 审计日志
- [ ] **安全专项落地**：prompt injection 防护、工具级 RBAC、输出审核通道、防刷风控
- [ ] 可观测：SkyWalking 全链路 + ELK 日志 + Prometheus/Grafana 告警 + 容量规划
- [ ] 灰度发布流程 + 故障自愈 + 备份容灾演练
- [ ] 成本治理：LLM 结果缓存/短上下文/分级模型兜底/预算告警
- [ ] 数据治理：数据质量校验、隐私脱敏、合规审查（版权/用户协议）
- [ ] MCP Server 对外开放（工具中心标准化）
- [ ] 评测门禁常态化：变更回归 + 效果日报

**验收指标**：
- 可用性 ≥ 99.9%；问答 P95 ≤ 8s / 推荐 P95 ≤ 100ms
- 故障演练 RTO ≤ 30min / RPO ≤ 5min
- 全部变更走灰度 + 评测门禁，无例外

---

## 6. 关键设计决策清单（需尽快拍板）

1. **内容解析 pipeline 立项** —— 最重前置依赖，影响 RAG 质量上限
2. **工具中心 schema** —— 列出全部工具的输入/输出契约、权限边界、幂等要求
3. **评测集与门禁指标** —— RAG/推荐/Agent 三套评测的达标线（§3.3）
4. **最小 Agent 链路 demo**（搜索 + 问答 + 引用）
5. **埋点事件模型** —— 推荐与画像都依赖，事后补非常痛苦
6. **LLM/向量库/Rerank 选型 POC** —— 各跑一个真实场景再对比
7. **离线平台与 GPU 资源方案** —— 特征/训练/serving 的自建 vs 租用（§3.1）

## 7. 风险清单

| 风险 | 等级 | 对策 |
|---|---|---|
| 内容解析质量问题（PDF 版式/EPUB 结构差异） | 高 | 尽早用真实图书样本验证解析率；建立质量抽检与人工修正通道 |
| LLM 幻觉导致错误引用 | 高 | 强制引用格式、检索片段交叉验证、低置信度拒答 |
| 图书版权合规（生成缩影内容/整本喂入） | 高 | 只做检索式引用，不做摘要镜像；法务审查输出策略 |
| Prompt injection / 工具越权调用 | 高 | §3.4 安全专项；工具参数白名单、高危工具二次确认 |
| 评测集建设滞后（无门禁即上线） | 高 | Phase 0 即启动标注；每次上线前回归评测 |
| 推荐冷启动（图书无行为数据） | 中 | 内容/图谱/编辑规则先行；冷启动人群策略与长尾配置 |
| LLM API SLA/可用性（供应商故障） | 中 | 模型网关多供应商/分级降级、本地小模型兜底、结果缓存 |
| GPU 资源缺口（embedding/rerank/精排 serving） | 中 | Phase 0 评估算力需求；先租用/共享卡，再私有化 |
| LLM 成本失控 | 中 | 缓存、短上下文、分级模型、预算告警 |
| 一条大链路延迟叠加（网关+Agent+多工具+LLM） | 中 | 流式、并行工具调用、超时与降级分层 |
| 用户隐私（画像/行为数据） | 中 | 脱敏存储、最小授权、合规审计 |

## 8. 建议立即执行的 6 件事

1. **内容解析 pipeline 立项**（任务单：样本库 → 解析率指标 → 定时任务）
2. **定义工具中心 schema**（MCP 工具清单文档，先写文档再写代码）
3. **RAG 评测集启动**（100 题黄金标注：问题-答案-引用位置）
4. **打通最小 Agent 链路**（搜索工具 + 问答 + 引用格式，一天 demo）
5. **定义埋点事件模型**（事件表：行为 → 特征 → 推荐）
6. **选型 POC 对比**（LLM function calling / 向量库 / Rerank 各一组真实场景）

## 9. 团队分工与人力估算

| 小组 | 职责 | 建议人数 |
|---|---|---|
| 内容与数据组 | 内容解析 pipeline/ETL/索引构建/评测集建设 | 2–3 |
| Agent 应用组 | 对话交互/编排/工具中心/生成引擎 | 2–3 |
| 搜索推荐组 | RAG 检索/推荐排序/离线特征与训练 | 2–3 |
| 平台与安全组 | 网关/可观测/安全/成本/运维 | 1–2 |

合计 **7–11 人**；MVP 阶段（Phase 0–1）**4–5 人**即可启动（内容 1–2 + Agent 1–2 + 检索 1 + 兼职平台）。

---

*文档版本：v0.2（2026-08）。v0.2 增补：端到端时序图、离线数据链路、模型网关与治理、评估与上线门禁、安全专项、目标服务拓扑、各 Phase 量化验收指标、团队分工、风险清单扩充。后续按 Phase 0–3 迭代更新。*
